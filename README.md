# Setup Environment

### Run docker
```
docker-compose up -d
```

### Composer

```
composer install
```

You need to configure the `.env` file

### Finally, Import Database

```
php artisan make:migration
```
